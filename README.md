# ConnecT

ConnecT is a Python 3.12 reference implementation for a deployment-focused Infrastructure-as-Code DSL.

ConnecT source files use the `.ct` extension and compile app/database deployment declarations into validated JSON.

## Example

```bash
python -m connet.main connet/examples/production.ct
```
